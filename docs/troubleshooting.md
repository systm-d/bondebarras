# Troubleshooting

Common symptoms and what to do about them, each entry following the same shape
— `Symptom → Cause → Resolution → Safety note`. Planned entries: no
organization visible, 401 and 403 errors, unreadable billing, unreadable
budgets, unreadable retention, a repository visible but not archivable, a
package version with no size, GitHub's secondary rate limits and the retries
that answer them, a terminal too small, an update refused because of a
checksum, and telling zero apart from unavailable in the JSON output.

> Not written yet — tracked in #26.

Until then, [authentication](authentication.md#what-degrades-when-something-cannot-be-read)
covers the permission-shaped failures family by family, which is where most of
these symptoms come from.
