# Releases and versioning

How bondebarras is released: the difference between a stable release and a
pre-release, the formats the release workflow produces, the architectures
supported, the checksums published alongside each asset, the policy on
Homebrew, the AUR and winget, and what Semantic Versioning promises here —
including whatever guarantees the `scan --json` schema carries before and
after `1.0.0`.

> Not written yet — tracked in #26.

Until then, [`CHANGELOG.md`](../CHANGELOG.md) records every released version in
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) format, and the
[Installation section of the README](../README.md#installation) states which
channels are actually published today.
